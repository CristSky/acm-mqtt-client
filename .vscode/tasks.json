{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/acm-mqtt-client/acm-mqtt-client.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}